import React from 'react';
import SettingsSection from '@/components/Settings/SettingsSection';
import SettingsHeading from './SettingsHeading';

const InvoiceTransactionHistory: React.FC = () => {
  return (
    <SettingsSection>
      <SettingsHeading>Invoice &amp; Transaction History</SettingsHeading>
      Invoice &amp; Transaction History Content
    </SettingsSection>
  );
};

export default InvoiceTransactionHistory;
